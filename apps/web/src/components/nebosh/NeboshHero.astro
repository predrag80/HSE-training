---
interface Props {
	eyebrow: string;
	title: string;
	image: string;
	titleId: string;
	scrollLabel: string;
	compact?: boolean;
}

const { eyebrow, title, image, titleId, scrollLabel, compact = false } = Astro.props;
---

<section
	class:list={['nebosh-hero', { 'nebosh-hero--compact-title': compact }]}
	style={`--nebosh-hero-image:url('${image}')`}
	aria-labelledby={titleId}
	data-nebosh-parallax
>
	<div class="nebosh-hero__overlay" aria-hidden="true"></div>
	<div class="nebosh-hero__content" data-nebosh-reveal="hero">
		<p>{eyebrow}</p>
		<h1 id={titleId}>{title}</h1>
	</div>
	<a class="nebosh-hero__scroll" href="#course-content" aria-label={scrollLabel}><span></span></a>
</section>
